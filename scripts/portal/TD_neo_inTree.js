/**
 *	@Author: iPoopMagic (David)
 */
function enter(pi) {
    if (pi.isQuestStarted(3719) || pi.getPlayer().isGM()) {
		pi.warp(240070010);
		pi.playerMessage(5, "Defeat Nex the Time Guard!");
    }
    if (pi.isQuestStarted(3724) || pi.getPlayer().isGM()) {
		pi.warp(240070020);
		pi.playerMessage(5, "Defeat Nex the Time Guard!");
    }
    if (pi.isQuestStarted(3730) || pi.getPlayer().isGM()) {
		pi.warp(240070030);
		pi.playerMessage(5, "Defeat Nex the Time Guard!");
    }
    if (pi.isQuestStarted(3736) || pi.getPlayer().isGM()) {
		pi.warp(240070040);
		pi.playerMessage(5, "Defeat Nex the Time Guard!");
    }
    if (pi.isQuestStarted(3742) || pi.getPlayer().isGM()) {
		pi.warp(240070050);
		pi.playerMessage(5, "Defeat Nex the Time Guard!");
    }
    if (pi.isQuestStarted(3748) || pi.getPlayer().isGM()) {
		pi.warp(240070060);
		pi.playerMessage(5, "Defeat Nex the Time Guard!");
    }
}